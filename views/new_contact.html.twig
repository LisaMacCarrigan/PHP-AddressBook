<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css">
        <link href="/css/styles.css" rel="stylesheet" type="text/css">
        <title>Contact Added</title>
    </head>
    <body>
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h2>You created a contact!</h2>
                    <h4>Full Name: </h4>
                    <p>{{ new_contact.fullName }}</p>
                    <h4>Phone: </h4>
                    <p> {{ new_contact.phone}} </p>
                    <h4>Address: </h4>
                    <p> {{ new_contact.fullAddress }} </p>
                    <form action="/" method="get">
                        <button type="submit" class="btn btn-success">Return Home</button>
                    </form>
                </div>
            </div>
        </div>
    </body>
</html>
